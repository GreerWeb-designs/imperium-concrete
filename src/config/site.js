// Central site constants. Keep canonical/base URL in one place so every page
// (and the sitemap/JSON-LD) stays consistent.
export const SITE_URL = "https://www.imperiumconcrete.com";
export const SITE_NAME = "Imperium Concrete";

// Phone, in the two formats the markup needs.
export const PHONE_TEL = "tel:18644476200";
export const PHONE_DISPLAY = "(864) 447-6200";
export const PHONE_E164 = "+1-864-447-6200";

// Default social-share image (served from /public).
export const DEFAULT_OG_IMAGE = `${SITE_URL}/og.png`;
